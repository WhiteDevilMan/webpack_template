extends ../components/layouts/main

block variables
  - var title = 'Webpack + Pug';
  - var description = 'Description for Webpack + Pug';
  - var pageClass = 'index-page';



mixin item(title, descr)
  h2.item-title= title
  p.item-descr= descr

block content
  section
    .container
      +item('Usage:', 'Add ycfn.pug, ycfn.scss and ycfn.js files to src/componets/ycfn')
      +item('Add to .js file:', "import './ycf.scss';")
      +item('Note:', 'ycfn - yourcomponentfoldername')

block script
  script(type='text/javascript').
    const foo = true;
    let bar = function() {};
    if (foo) {
    bar(1 + 5)
    }